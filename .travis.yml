dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install .
  - pip install codecov coverage

script:
  - coverage run tests/initial-test.py

after_success:
  - codecov